@model IEnumerable<SUP_CORE.DTO.QLT_PODTO>
@{
    Layout = null;
}
<div class="table-responsive font">
    <table class="table table-bordered" id="dataTable2" width="100%" cellspacing="0">
        <thead>
            <tr class="text-wrap small">
                <th>Action</th>
                <th>OrderNumber</th>
                <th>LineNo</th>
                <th>Item Code</th>
                <th>Item Description</th>
            </tr>
        </thead>
        <tbody class="text-nowrap">
            @foreach (var item in Model)
            {
                <tr class="nowrap small" style=" text-transform: uppercase;">
                    <td>
                        @Html.ActionLink("Edit", "QLTEdit", "PO", new { id = item.Id }, new { @class = "text-success" })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Order_Number)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Line_Number)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Item)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>